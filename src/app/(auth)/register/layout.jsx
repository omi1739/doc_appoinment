export const metadata = {
  title: "Register | DocAppoint",
  description: "Create an account on DocAppoint to easily book and manage your doctor appointments.",
};

export default function RegisterLayout({ children }) {
  return <>{children}</>;
}
