export const metadata = {
  title: "Login | DocAppoint",
  description: "Sign in to your DocAppoint account to manage appointments and update your profile.",
};

export default function LoginLayout({ children }) {
  return <>{children}</>;
}
