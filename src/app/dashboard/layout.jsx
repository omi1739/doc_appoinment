export const metadata = {
  title: "Dashboard | DocAppoint",
  description: "Manage your doctor appointments, view upcoming bookings, and update your personal profile.",
};

export default function DashboardLayout({ children }) {
  return <>{children}</>;
}
